# GlowGuide environment variables

# The current MVP uses local SQLite and does not require secrets.
# Add these later if migrating to Supabase or another hosted database.

SUPABASE_URL=
SUPABASE_KEY=
DATABASE_URL=
